const HeroSection = () => {
      return (
            <div className="bg-primary-400 h-[calc(100vh - 96px)]">
                  <h2>Welcome to the HeroSection page</h2>
            </div>
      );
};

export default HeroSection;
