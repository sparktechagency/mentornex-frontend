import ProfileInformation from '@/components/shared/ProfileInformation';

const MenteeProfile = () => {
      return (
            <div>
                  <ProfileInformation />
            </div>
      );
};

export default MenteeProfile;
