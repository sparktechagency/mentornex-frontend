import ProfileInformation from '@/components/shared/ProfileInformation';

const MentorProfile = () => {
      return (
            <div>
                  <ProfileInformation />
            </div>
      );
};

export default MentorProfile;
